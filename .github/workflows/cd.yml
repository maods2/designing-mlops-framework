# CD workflow — DISABLED: uncomment when ready to publish to JFrog and tag version for prod.
#
# name: CD
#
# on:
#   release:
#     types: [published]
#   workflow_dispatch:
#     inputs:
#       version:
#         description: "Version to publish (e.g. 0.3.0)"
#         required: true
#
# jobs:
#   publish:
#     name: Publish to JFrog Artifactory
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.10"
#
#       - name: Install build tools
#         run: pip install build twine
#
#       - name: Build distribution
#         run: python -m build mlplatform/
#
#       - name: Publish to JFrog
#         env:
#           JFROG_URL: ${{ secrets.JFROG_URL }}
#           JFROG_USER: ${{ secrets.JFROG_USER }}
#           JFROG_PASSWORD: ${{ secrets.JFROG_PASSWORD }}
#         run: |
#           twine upload \
#             --repository-url "$JFROG_URL" \
#             --username "$JFROG_USER" \
#             --password "$JFROG_PASSWORD" \
#             mlplatform/dist/*
#
#       - name: Tag version for prod deployment
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "github-actions[bot]@users.noreply.github.com"
#           git tag "v${{ github.event.inputs.version || github.event.release.tag_name }}"
#           git push origin --tags
