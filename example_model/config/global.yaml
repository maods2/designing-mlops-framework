# Global config profile - baseline settings applied to all environments.
# Load order: global is first, later profiles (dev, local, prod) override.
log_level: INFO
base_path: ./artifacts
profile: local
