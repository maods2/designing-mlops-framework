# Global config profile - baseline settings for trend model v2.
log_level: INFO
base_path: ./artifacts
profile: local

# Optional: for GCS storage (when using cloud-* profiles)
# bucket: my-artifact-bucket
# artifact_prefix: models
# gcp_project: my-gcp-project
