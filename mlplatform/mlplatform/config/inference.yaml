# Step config template for inference - runner, storage, and tracking backends per environment.
# Lives in mlplatform/config/ (framework-level config).
# Module and class are provided by the DAG YAML; this file defines runner/storage/etb env mapping.
name: inference
type: inference
envs:
  dev:
    runner: LocalRunner
    storage: LocalFileSystem
    etb: LocalJsonTracker
  local_spark:
    runner: LocalSparkRunner
    storage: LocalFileSystem
    etb: LocalJsonTracker
    runner_config:
      direct: true
  prod:
    runner: LocalSparkRunner
    storage: LocalFileSystem
    etb: LocalJsonTracker
    runner_config:
      direct: false
