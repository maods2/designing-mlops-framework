# GCS config profile - use with cloud-batch, cloud-online, or cloud-train profiles.
# Merge with: config: [global, gcs]
log_level: INFO
base_path: ./artifacts
bucket: my-artifact-bucket
artifact_prefix: models
gcp_project: my-gcp-project
